<% match = local_assigns[:match] %>
<div class="ledger-surface p-6 md:p-10">
  <div class="flex justify-center mb-4">
    <%= match_status_badge(:live) %>
  </div>
  <h1 class="text-2xl md:text-4xl font-black italic tracking-tighter uppercase text-center mb-2">
    <%= match[:home] %> vs <%= match[:away] %>
  </h1>
  <p class="text-center text-[var(--text-muted)] font-mono text-sm mb-8">
    <%= match[:league] %> R<%= match[:round] %> · <%= match[:minute] %>'
  </p>
  <div class="flex flex-col md:flex-row items-center justify-center gap-6 md:gap-12 text-center">
    <div class="flex-1">
      <p class="text-lg md:text-xl font-bold italic mb-2"><%= match[:home] %></p>
      <p class="text-3xl md:text-4xl font-black font-mono text-[var(--accent-var)]"><%= match[:score_home] %></p>
    </div>
    <div class="font-mono font-black text-2xl text-[var(--text-muted)]">-</div>
    <div class="flex-1">
      <p class="text-lg md:text-xl font-bold italic mb-2"><%= match[:away] %></p>
      <p class="text-3xl md:text-4xl font-black font-mono"><%= match[:score_away] %></p>
    </div>
  </div>
  <div class="mt-10 pt-8 border-t border-[var(--ledger-border)] text-center">
    <p class="font-mono text-sm text-[var(--text-muted)]"><%= match[:venue] %></p>
    <p class="text-xs text-[var(--text-muted)] mt-4">라이브 경기 UI — 순간·댓글 등은 이후 세부 기능에서 연결</p>
  </div>
</div>
