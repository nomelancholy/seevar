# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config and vendored gems (project uses bundle path vendor/bundle).
.bundle/
/vendor/bundle/

# Ignore all environment files (secrets).
.env
.env.*
!.env.example

# Ignore Rails master key and credential keys.
/config/master.key
/config/credentials/*.key

# Ignore Kamal deploy secrets (passwords, env vars).
/.kamal/secrets

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles.
/tmp/pids/*
!/tmp/pids/.keep

# Ignore storage (uploaded files, Active Storage).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/.keep

# Ignore asset builds (Tailwind, etc.).
/node_modules/
/app/assets/builds/*
!/app/assets/builds/.keep
/public/assets
/public/packs
/public/packs-test

# Ignore SQLite (this app uses PostgreSQL; leave in for compatibility).
*.sqlite3
*.sqlite3-*
/db/*.sqlite3
/db/*.sqlite3-*

# Ignore editor and OS files.
*.swp
*.swo
*~
.byebug_history
.DS_Store
.idea/

# Ignore YARD and docs.
/doc/
/ydoc/
/.yardoc/

# Ignore local overrides (optional; uncomment if you use them).
# /config/database.local.yml
# /config/cache.local.yml
# /config/queue.local.yml
